#![allow(dead_code)]

const PHDR_SIZE: usize = 56;
const SHDR_SIZE: usize = 64;

// pub fn load_phdrs(img: *mut u8, vec: &Vec<u8>, start: usize, nbr: usize) {

// }

// pub fn load_shdrs(img: *mut u8, vec: &Vec<u8>, start: usize, nbr: usize) {

// }